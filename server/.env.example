ARCH_WP_API_PORT="5086"
# Optional: absolute or cwd-relative SQLite file path
# ARCH_WP_SQLITE_URI="sqlite:./db.sqlite3"
# Optional: data directory used when ARCH_WP_SQLITE_URI is unset (default: ~/.arch-push-notifications)
# ARCH_WP_DATA_DIR="/var/lib/arch-push-notifications"
